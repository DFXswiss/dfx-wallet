diff --git a/node_modules/expo-image/ios/ContentPosition.swift b/node_modules/expo-image/ios/ContentPosition.swift
index 73c34ca..5a74b52 100644
--- a/node_modules/expo-image/ios/ContentPosition.swift
+++ b/node_modules/expo-image/ios/ContentPosition.swift
@@ -10,7 +10,7 @@ typealias ContentPositionValue = Either<Double, String>
  - Note: Its intention is to behave like the CSS [`object-position`](https://developer.mozilla.org/en-US/docs/Web/CSS/object-position) property.
  */
 struct ContentPosition: Record {
-  static let center = Self()
+  static var center: ContentPosition { Self() }
 
   @Field
   var top: ContentPositionValue?
diff --git a/node_modules/expo-image/ios/ImageLoadTask.swift b/node_modules/expo-image/ios/ImageLoadTask.swift
index 75f30c0..6a31d87 100644
--- a/node_modules/expo-image/ios/ImageLoadTask.swift
+++ b/node_modules/expo-image/ios/ImageLoadTask.swift
@@ -2,7 +2,7 @@
 
 import ExpoModulesCore
 
-internal final class ImageLoadTask: SharedObject {
+internal final class ImageLoadTask: SharedObject, @unchecked Sendable {
   private let source: ImageSource
   private let options: ImageLoadOptions
   private var task: Task<UIImage, any Error>?
@@ -13,12 +13,8 @@ internal final class ImageLoadTask: SharedObject {
     super.init()
   }
 
-  func load() async throws -> UIImage {
-    let task = self.task ?? Task { [source, options] in
-      return try await ImageLoader.shared.load(source, options: options)
-    }
-    self.task = task
-    return try await task.value
+  nonisolated func load() async throws -> UIImage {
+    return try await ImageLoader.shared.load(source, options: options)
   }
 
   func abort() {
